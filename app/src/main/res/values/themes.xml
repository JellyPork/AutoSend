<resources>
    <!-- Base theme. Compose drives the real colors; this just provides a splash-safe shell. -->
    <style name="Theme.AutoSend" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent shell for WakeActivity so it doesn't show any UI over the lock screen. -->
    <style name="Theme.AutoSend.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
